# Audio SMB Sync 向导界面

## 1. 视觉主题与氛围

可靠、安静、直接。界面像一张清晰的设置清单，不像工程调试面板。每屏只承担一个任务。

## 2. 色板与角色

- 主色：深紫 `#6750A4`，用于当前步骤和主操作。
- 背景：暖白 `#FAF8FC`。
- 表面：白色 `#FFFFFF`。
- 成功：深绿；警告：琥珀；错误：Material error。
- 禁止紫蓝渐变、霓虹和纯黑。

## 3. 排版规则

使用 Android 系统中文字体。标题 500–600，正文 400；中文不使用斜体。正文至少 16sp，行距留足。

## 4. 组件样式

- 顶部使用水平步骤指示器，完成、当前、未解锁三态明确。
- 每屏一个主按钮，异步时按钮原位显示进度且禁止重复点击。
- 错误使用靠近操作区的内联提示，包含可执行的下一步。
- 表单字段保留可见标签，不以占位符代替标签。

## 5. 布局原则

移动端单列，24dp 页面边距，组内 8–12dp，组间 20–28dp。未来步骤不提前展示，已完成步骤允许返回修改。

## 6. 深度层级

只给当前任务容器轻微表面层级；状态条和日志用分隔线与留白组织，不将所有内容卡片化。

## 7. Do / Don't

- Do：立即反馈、清楚说明阻塞原因、错误后可重试。
- Do：成功后自动解锁下一步。
- Don't：按钮点击无反应、一次展示所有配置、用灰色禁用却不解释原因。
- Don't：使用左侧竖线作为选中或强调装饰。

## 8. 响应式

以手机竖屏为主；宽屏仍保持单列内容区，最大宽度由页面内边距控制，避免超宽表单。

## 9. Motion 哲学

动效只用于步骤切换和操作反馈，时长不超过 220ms。加载使用原位进度指示，不阻塞整屏；系统减少动态效果时保留状态切换、移除位移。
